# skills/ — 온디맨드 도메인 지식

자주 안 쓰지만 필요할 때만 로드할 상세 지식을 둔다. 각 스킬은 폴더 + `SKILL.md`:

```
.claude/skills/<name>/SKILL.md
```

`SKILL.md` 상단 frontmatter에 `name`, `description`(언제 로드할지 판단 기준)을 둔다.
항상 참인 규칙은 여기 말고 `CLAUDE.md`, 경로스코프 언어 규칙은 `.claude/rules/`.

(아직 정의된 스킬 없음 — 필요 시 추가.)

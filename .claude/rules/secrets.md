# 시크릿 규칙

상시 적용 (경로 스코프 없음).

- 시크릿(API 키, 토큰, 비밀번호, 접속 문자열)은 `.env`에서만 로드한다.
- `.env`는 gitignore에 둔다. 절대 커밋하지 않는다.
- 키·토큰·시크릿 값을 코드, 로그, 콘솔 출력, 커밋, 테스트 픽스처에 절대 포함·출력하지 않는다.
- 예시는 `.env.example`에 키 이름만 두고 값은 비운다.
- 시크릿이 필요한 설정은 사람 확인 항목으로 사용자에게 요청한다.

# <프로젝트명>

<!--
  이 파일은 범용 하네스 템플릿이다. 프로젝트마다 <...> 슬롯을 채우고, 안내 주석(<!-- -->)은 채운 뒤 지운다.
  안내 주석은 컨텍스트에 로드되지 않으므로(HTML 주석 제거됨) 남겨둬도 토큰은 안 먹지만, 완성본은 깔끔하게 지운다.

  작성 원칙:
  1) 매 세션 로드된다 → "항상 참인 것"만 넣는다.
  2) 짧게 (~200줄 이하). 길면 클로드가 무시한다.
  3) 코드만 봐서 알 수 있는 건 적지 않는다.
  4) 각 줄에 물어봐라: "이걸 지우면 클로드가 실수할까?" 아니면 삭제.
  5) 상세는 @ 포인터로 링크하고 인라인하지 않는다.

  범례:
  - 그냥 문장 = 범용 규칙. 대개 그대로 둔다.
  - <꺾쇠> = 프로젝트마다 채우는 슬롯.
  - "없으면 이 절/줄 삭제" 표시가 있으면, 해당 없을 때 통째로 지운다.
-->

## What — 이 프로젝트가 무엇인가
<!-- 한두 줄. 클로드가 목적/도메인을 오해하지 않을 최소치만. -->
- 한 줄: <이 프로젝트가 무엇을 하는지 한 문장>
- 스택(선언): <언어/프레임워크 · DB · 외부 API · 배포>
- 진입점: <백엔드 메인 · 프론트 진입 파일>

## Why — 코드만 봐선 모를 결정
<!-- 코드를 읽어도 안 나오는 의도적 결정만. 언어/프레임워크 관례는 적지 않는다. 없으면 이 절 삭제. -->
- <지켜야 할 결정/제약 1 — "왜"보다 "무엇을 지켜야 하는지">
- <지켜야 할 결정/제약 2>

## How — 빌드 / 실행 / 검증
### 명령어 (클로드가 추측 못 하는 것)
<!-- 표준과 다르거나 프로젝트 고유한 것만. 검증 명령은 @docs/testing.md와 일치시킨다. -->
- 실행: <dev/run 명령 + 포트> · <build 명령>
- 검증: <lint · typecheck · test 명령 — 아직 없으면 "미정">

### 워크플로 (범용 — 대개 그대로 둔다)
- 큰 변경은 Plan Mode로 승인 후 진행. 한 번에 하나의 task만.
- 코드 변경 후 변경 범위의 검증 게이트를 통과시킨다 → @docs/testing.md. 못 돌렸으면 "안 돌림" 명시.
- 이력/되돌리기는 git 커밋 + `/rewind`.

## 코드 스타일 (언어 기본값과 다른 것만)
- 상세 규칙은 경로 스코프로 자동 로드 → `@.claude/rules/`.
<!-- 여기엔 요약 1~2줄만. 언어 기본 관례와 다른 점만. 상세는 rules/로. -->
- 요약: <언어별 핵심 스타일 한 줄>

## 경계 (Boundaries) — 건드리지 말 것
<!-- 아래 3줄은 범용. 유지한다. -->
- **IMPORTANT**: API 키/시크릿 절대 하드코딩 금지. `.env`만 사용 (@.claude/rules/secrets.md).
- 요청/현 task 범위 밖 구현·추상화·문서 확장 금지(스코프 크리프).
- 작성한 코드는 현재 task 범위 밖에서 임의 리팩터하지 않는다.
<!-- 프로젝트 고유 강제/금지가 있으면 아래에 추가. 없으면 이 줄 삭제. -->
- **YOU MUST**: <프로젝트 고유 강제 규칙 — 예: 키 없을 때 데모 모드 동작, 특정 실호출 금지>

## 포인터 (Pointers) — 상세는 링크로
<!-- 상시 인라인 금지, 링크만. 프로젝트에 없는 문서 줄은 삭제. -->
- 도메인 상세: @docs/domain.md   <!-- 도메인 규칙이 복잡할 때만. 없으면 삭제. -->
- 아키텍처 상세: @docs/architecture.md
- 검증 게이트: @docs/testing.md
- 언어 규칙: @.claude/rules/
- 환경변수 (키 이름만 — 값은 `.env`):
  - 시크릿: <KEY_A>, <KEY_B>
  - 기본값(여기 1곳에만 기록): <설정 키>=<기본값>   <!-- 없으면 삭제 -->

## 사람 확인 (런타임/통합 — 클로드가 검증 불가)
<!-- 자동 명령(lint/test 등)으로 검증 가능한 건 여기 적지 않는다(→ 검증 게이트). 없으면 이 절 삭제. -->
- <런타임/통합 확인 항목 — 예: 스케줄러가 실제 시각에 구동, 외부 엔드포인트 실응답>
- <수동 시크릿/외부 콘솔 설정 항목>

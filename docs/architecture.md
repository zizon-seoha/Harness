# 아키텍처 상세 — <프로젝트명>

<!--
  범용 템플릿. <...> 슬롯을 채우고 안내 주석은 지운다.
  여기엔 "구조/폴더/영향도"만. 도메인 규칙은 domain.md, 검증은 testing.md.
  코드만 봐서 바로 아는 건 적지 않는다(폴더가 자명하면 생략).
-->

CLAUDE.md에서 `@docs/architecture.md`로 참조.

## 디렉토리
<!-- 모노레포/단일 등 레이아웃과 각 폴더 책임. 클로드가 새 파일을 엉뚱한 곳에 두지 않을 최소치. -->
- <영역 A(예: 프론트)>: `<경로>` — `<하위폴더>`(책임) · `<하위폴더>`(책임)
- <영역 B(예: 백엔드)>: `<경로>` — `<하위폴더>`(책임) · <마이그레이션/설정 위치>

## 중요 컴포넌트
<!-- 코드 전체를 훑지 않아도 어디를 봐야 하는지. 자명한 건 생략. 없으면 이 절 삭제. -->
- <컴포넌트/모듈> — <역할>
- <공유 유틸/경계 모듈> — <역할>

## 변경 시 함께 확인 (영향도)
<!-- "X를 바꾸면 Y도 함께 봐야 한다" 형태. 흩어진 결합만. 없으면 이 절 삭제. -->
- <규칙 X 변경> → <함께 고칠 곳> + <관련 문서>
- 환경변수 키 추가 → <설정 파일> + `.env` + CLAUDE.md 포인터.

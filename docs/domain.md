# 도메인 상세 — <프로젝트명>

<!--
  범용 템플릿. 실수 방지용 비즈니스 규칙의 단일 출처.
  여기 넣을 것: 코드를 읽어도 "왜/어떤 규칙인지" 안 나오는 도메인 규칙 — 숫자 경계, 상태 전이,
  시간/스케줄 규칙, 집계 방식, 외부 API 계약 등. 클로드가 자주 틀리는 규칙을 우선.
  여기 넣지 말 것: 언어/프레임워크 관례, 폴더 구조(→ architecture.md), 검증 명령(→ testing.md).
  도메인이 단순하면 이 문서 자체가 필요 없다. 필요 없으면 파일과 CLAUDE.md 포인터를 함께 삭제.
  아래 절 제목은 예시다. 프로젝트 도메인에 맞게 바꾼다.
-->

CLAUDE.md에서 `@docs/domain.md`로 참조.

## <핵심 규칙 그룹 1 — 예: 스케줄/시간>
<!-- 시각·주기·마감처럼 코드에 흩어지기 쉬운 규칙. -->
- <규칙>

## <핵심 규칙 그룹 2 — 예: 점수/수치 경계>
<!-- 최소/최대, 반올림 자리수, 계산식처럼 어긋나면 티 안 나는 규칙. -->
- <규칙>

## <핵심 규칙 그룹 3 — 예: 집계/랭킹>
<!-- 어떤 데이터를 어떤 기준으로 묶고 무엇만 반영하는지. -->
- <규칙>

## 외부 API / 계약
<!-- 외부 의존의 고정 계약(엔드포인트 의미, 언어/코드 매핑, 키 없을 때 동작). 없으면 삭제. -->
- <서비스> — <계약/동작>

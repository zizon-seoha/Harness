# 검증 게이트 — <프로젝트명>

<!--
  범용 템플릿. "코드 변경 후 무엇을 통과시켜야 하나"의 단일 출처.
  아래 원칙/결정화/사람확인 절은 범용 규칙 — 대개 그대로 둔다.
  "현재 게이트"의 <...> 명령만 프로젝트마다 채운다.
-->

CLAUDE.md에서 `@docs/testing.md`로 참조.

## 원칙 (범용)
- 변경 범위에 해당하는 게이트는 **반드시** 통과시킨 뒤 완료 보고한다.
- 게이트를 못 돌렸으면 "안 돌림"이라고 명시한다(통과로 가장하지 않음).
- 자동 명령으로 검증 가능한 건 사람 확인으로 미루지 않는다.

## 현재 게이트 (셋업 진행에 따라 갱신)
<!-- 변경 범위별로 "통과해야 하는 명령"을 적는다. 아직 없으면 "없음". 셋업하며 여기 갱신. -->
- <영역 A(예: 프론트)>: `<검증 명령>` 통과.
  - <추가 lint/test 명령 — 없으면 "없음">
- <영역 B(예: 백엔드)>: `<검증 명령>` 통과.
  - <추가 test 명령 — 없으면 "없음">

## 결정화 (권장, 범용)
- 게이트가 확정되면 `.claude/settings.json` 훅(PreToolUse/Stop) 또는 CI로 박아 누락을 막는다.
- 산문 "검증해라"에 의존하지 않는다.

## 사람 확인 (Claude가 검증 불가, 범용)
- 런타임/통합 항목은 CLAUDE.md "사람 확인" 절을 단일 출처로 참조한다.
